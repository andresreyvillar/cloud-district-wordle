# Tareas — chore-contraste-del-compartir

1. `v2/css/styles.css`: `.compartir-texto` con `--liga-tablero`, `--liga-tinta` y borde `--borde`.
2. Medir el contraste en claro y oscuro con el texto para compartir a la vista.

```bash
node --test tests/
```
